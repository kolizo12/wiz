eksctl create nodegroup \
  --cluster kolizo-cluster \
  --region us-west-2 \
  --name kolizo-mangegroup \
  --node-type t2.medium \
  --nodes 3 \
  --nodes-min 2 \
  --nodes-max 4 \
  --ssh-access \
  --ssh-public-key webapp
